ready() vs load()
ready() = will be activated once the entire dom is loaded except media and iframes;
load() = will be activated on the entire page is loaded including(dom + media + iframes)
jquery-selector
-- $(document),$("h1)

hide vs remove;
hide-> gets removed from screen as well as dom
remove=> gets removed from screen as well as dom.